# CaseFlow — Sitemap

/ → Dashboard (high-level stats + recent cases)
/cases → Case list view
/cases/[id] → Case detail page (case info + client info + activity timeline)

/clients → Clients list (future)
/clients/[id] → Client detail (linked cases) (future)

/settings → App/agency settings (future)
